# Leave empty (or omit) for same-origin production deploys where the backend
# is served from the same domain under /api. Set to the backend's local URL
# for development.
VITE_API_BASE_URL=http://localhost:8000
